import {StyleSheet} from 'react-native';
import {
  heightPercentageToDP,
  widthPercentageToDP,
} from 'react-native-responsive-screen';

const styles = StyleSheet.create({
  container: {
    width: '35%',
    alignSelf: 'center',
  },
  lable: {textAlign: 'center', fontWeight: 'bold'},
});

export default styles;
